name("Simple test step with invalid complex function where parent has more properties"){
    payload
    {
        toJson()
        {
            command = "go"
        }
        p1 = something
    }
}