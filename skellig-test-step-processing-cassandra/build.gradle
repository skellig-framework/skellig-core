plugins {
    id 'java'
}

dependencies {
    compile 'com.datastax.cassandra:cassandra-driver-core:3.0.0'

    compile project(':skellig-test-step-processing-db')

    testImplementation group: 'org.junit.platform', name: 'junit-platform-runner'
    testImplementation group: 'org.junit.jupiter', name: 'junit-jupiter-api'
    testImplementation group: 'org.mockito', name: 'mockito-all'
    testRuntimeOnly group: 'org.junit.jupiter', name: 'junit-jupiter-engine'

    testCompile 'org.testcontainers:testcontainers:1.0.0'
    testCompile "org.testcontainers:junit-jupiter:1.0.0"
    testCompile "org.testcontainers:cassandra:1.0.0"
}

test {
    useJUnitPlatform()
}