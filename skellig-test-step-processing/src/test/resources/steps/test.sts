# this is empty test step to make unit test pass

name(Run command (.*))
 url(/a/b/c)
 method(POST)
 payload(
    json(
        {
          command: "go"
        }
    ))
 validate(
    json_path(/r/id, get(id))
    json_path(/r/name, $1)
    json_path(/r/success, true)
 )


name(Test rmq send)
 send_channel(CHN1)
 receive_channel(CHN2)
 message(
    command(
          name: "go"
          params: 1,2,3
          date: now()
    )
 )
 validate(
    contains_expected_values(
       contains(success)
       contains(go)
       regex(.*get(id).*)
    )
    has_fields(
      f1: v1
      f2: get(ff11)
    )
 )


